package hero;

// an enum class for different character types to facilitate creating new characters
public enum HeroType {
    // if new character classes are added, a corresponding types should be added here
    WARRIOR,
    RANGER,
    MAGE
}
